export function CategoriesHeader() {
  return (
    <section>
      <h1 className="text-3xl font-semibold tracking-tight text-[var(--foreground)]">Categories</h1>
      <p className="mt-1 text-sm text-[var(--foreground-muted)]">
        Organize how transactions are classified across Athena.
      </p>
    </section>
  );
}
